---
description: Placeholder skill for the core harness plugin. Carries no guidance and is not model-invoked.
disable-model-invocation: true
---

# Placeholder

This skill is a **placeholder**. It exists only so the `core` plugin has a valid
component and the marketplace can be validated and installed end to end before
any real harness content is written.

It carries no instructions, conventions, or guidance. If you are reading this
because it was invoked, there is nothing to do — say so and continue with the
task at hand.

Replace this directory with real skills when the shared harness gets its first
content. See the repository `README.md` for the plugin layout.
